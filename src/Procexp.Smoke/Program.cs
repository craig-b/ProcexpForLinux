// Headless smoke-checker for the data layer, mirroring Sources/ProcexpSmoke in
// the macOS project. It exercises every provider without a GUI so the sampling
// engine is provable on its own.

Console.WriteLine("procexp-smoke: scaffold only, providers not yet wired.");
return 0;
